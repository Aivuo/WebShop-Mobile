<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>@ViewBag.Title - My ASP.NET Application</title>
    @Styles.Render("~/Content/css")
    @Scripts.Render("~/bundles/modernizr")

</head>
<body>
    <div class="container">
        <div class="row">
            <div class="col-xs-6" id="Title">
                @Ajax.ActionLink("ShopMobile", "Index", "Home", new AjaxOptions {
               UpdateTargetId = "MainBody", HttpMethod = "get", InsertionMode = InsertionMode.Replace },
                     new { @class = "navbar-brand" })
            </div>
            <div class="col-xs-4">
                @Html.Partial("_LoginPartial")
            </div>
        </div>
    </div>
   
    <nav class="navbar sticky-top navbar-expand-lg navbar-dark bg-primary ">
        <div class="collapse navbar-collapse ">
            <ul class="navbar-nav container justify-content-center">
                <li class="nav-item active ">
                    @*<a class="nav-link">*@
                    @Ajax.ActionLink("Hem", "Index", "Home", new AjaxOptions
               {
                   UpdateTargetId = "MainBody",
                   HttpMethod = "get",
                   InsertionMode = InsertionMode.Replace
               }, new { @class = "navbar-text" })
                    @*</a>*@
                </li>
                <li class="nav-item">
                    @*<a class="nav-link">*@
                    @Ajax.ActionLink("Kategorier", "Index", "Home", new AjaxOptions
               {
                   UpdateTargetId = "MainBody",
                   HttpMethod = "get",
                   InsertionMode = InsertionMode.Replace
               }, new { @class = "navbar-text" })
                    @*</a>*@
                </li>
                <li class="nav-item">
                    @*<a class="nav-link">*@
                    @Ajax.ActionLink("Om oss", "About", "Home", new AjaxOptions
               {
                   UpdateTargetId = "MainBody",
                   HttpMethod = "get",
                   InsertionMode = InsertionMode.Replace
               }, new { @class = "navbar-text" })
                    @*</a>*@
                </li>
                <li class="nav-item">
                    @*<a class="nav-link">*@
                    @Ajax.ActionLink("Kontakta oss", "Contact", "Home", new AjaxOptions
               {
                   UpdateTargetId = "MainBody",
                   HttpMethod = "get",
                   InsertionMode = InsertionMode.Replace
               }, new { @class = "navbar-text" })
                    @*</a>*@
                </li>
            </ul>
        </div>
    </nav>
    @*<div class="navbar navbar-in navbar-inverse navbar-fixed-top">
            <div class="container">
                <div class="navbar-header">
                    <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse">
                        <span class="icon-bar"></span>
                        <span class="icon-bar"></span>
                        <span class="icon-bar"></span>
                    </button>
                    @Ajax.ActionLink("ShopMobile", "Index", "Home", new AjaxOptions {
                   UpdateTargetId = "MainBody", HttpMethod = "get", InsertionMode = InsertionMode.Replace },
                   new { @class = "navbar-brand" })
                    @Html.ActionLink("ShopMobile", "Index", "Home", new { area = "" }, new { @class = "navbar-brand" })
                </div>
                <div class="navbar-collapse collapse">
                    <ul class="nav navbar-nav">
                        <li>@Ajax.ActionLink("Home", "Index", "Home", new AjaxOptions
                       {
                           UpdateTargetId = "MainBody",
                           HttpMethod = "get",
                           InsertionMode = InsertionMode.Replace
                       },
                   new { @class = "navbar-brand" }))</li>
                        <li>@Ajax.ActionLink("About", "About", "Home", new AjaxOptions
                       {
                           UpdateTargetId = "MainBody",
                           HttpMethod = "get",
                           InsertionMode = InsertionMode.Replace })</li>

                        <li>@Ajax.ActionLink("Contact", "Contact", "Home", new AjaxOptions {
                           UpdateTargetId = "MainBody",
                           HttpMethod = "get",
                           InsertionMode = InsertionMode.Replace })</li>
                    </ul>
                    @Html.Partial("_LoginPartial")
                </div>
            </div>
        </div>*@
    <div id="MainBody" class="container body-content">
        @RenderBody()
        <hr />
    </div>
        <footer class="text-sm-center">
            <p>&copy; @DateTime.Now.Year - ShopMobile</p>
        </footer>

    @Scripts.Render("~/bundles/jquery")
    @Scripts.Render("~/bundles/bootstrap")
    @RenderSection("scripts", required: false)
</body>
</html>
